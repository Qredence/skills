# Context7 MCP
- The Context7 MCP server was successfully installed and tested.
- It provides `resolve-library-id` and `query-docs` tools for querying documentation.
- When querying for libraries, it returns matching options from Context7 (e.g., query 'how to use hooks' for library 'react' returns '/websites/react_dev').